# GitHub Pages site for PolicyShift
# Enable: Settings → Pages → Deploy from branch `main` → folder `/docs` 
# Then open: https://ishikanahar.github.io/policyshift/site/

This directory is generated/synced from `apps/web` via `python scripts/export_portfolio.py`.
